/**
 * Feature flag for Didactic Materials & PPT Presentation Optimizer.
 * Active across all environments (portal facilitador & dashboard).
 */
export function isMaterialesEnabled(): boolean {
  return true;
}

